import 'package:flutter/material.dart';

class WeeklyAnalyticsPage extends StatelessWidget {
  const WeeklyAnalyticsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child: Column(
        children: [
          AppBar(),
        ],
      ),
    );
  }
}
